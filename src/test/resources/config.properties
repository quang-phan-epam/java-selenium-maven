# ── Application ───────────────────────────
base.url=https://www.amazon.com

# Credentials — never commit real values.
# GitHub Actions injects these via Repository Secrets:
#   Settings → Secrets → Actions → New repository secret
# mvn test -Duser.email=${{ secrets.AMAZON_EMAIL }} -Duser.password=${{ secrets.AMAZON_PASSWORD }}
user.email=quangpc07s@gmail.com
user.password=Test@123Invalid

# ── Browser ────────────────────────────────
# Overridden to 'chrome' + headless=true by GitHub Actions workflow
browser=chrome
headless=false

# ── Timeouts (seconds) ─────────────────────
implicit.wait=0
explicit.wait=15
page.load.timeout=30
